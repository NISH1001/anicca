/* anicca new-tab configuration.
 *
 * This is an external file (not an inline <script>) because the extension's
 * Manifest V3 CSP — script-src 'self' — blocks inline scripts.
 *
 * It carries NO personal data: the engine ships neutral defaults, and a first
 * run with no saved config opens the setup panel so each person sets their own.
 */
window.ANICCA_DEFAULT_LENS = 'stillness'; // open a new tab on the stillness lens
window.ANICCA_ONBOARD = true;             // first run → open setup to ask for birth year
